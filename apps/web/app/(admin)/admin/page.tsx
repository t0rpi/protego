/**
 * Admin placeholder ("/admin"). M0 scope: empty page only — pricing
 * engine config, service switches per city, audit log land starting M2+.
 */
export default function AdminHomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-sm text-[var(--text-secondary)]">
        PROTEGO — admin (M0 placeholder)
      </p>
    </main>
  );
}
