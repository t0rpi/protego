/**
 * Client-web placeholder ("/"). M0 scope: empty page only — the booking
 * flow, Shield surfaces etc. land starting M2/M6, and the mobile app
 * (apps/mobile) is the primary client surface (design/HANDOFF.md).
 */
export default function ClientHomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-sm text-[var(--text-secondary)]">
        PROTEGO — client-web (M0 placeholder)
      </p>
    </main>
  );
}
