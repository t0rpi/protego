{
  "name": "@protego/ui",
  "version": "0.1.0",
  "private": true,
  "description": "PROTEGO design tokens + component library (web + React Native).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "tokens:sync": "node ./scripts/sync-tokens.mjs",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests yet — packages/ui\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^9.17.0",
    "typescript": "^5.7.2"
  }
}
