export { tokens } from "./tokens";
export type { ProtegoTokens } from "./tokens";

// Component library (Button, SOSButton, StatusPill, QuoteBox, Disclaimer112, …
// per design/HANDOFF.md §3) is scaffolded starting M1+, alongside the screens
// that consume it. M0 ships tokens only.
